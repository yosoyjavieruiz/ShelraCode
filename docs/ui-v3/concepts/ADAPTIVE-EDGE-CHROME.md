# Concept C — Adaptive Edge Chrome (rejected as primary)

```text
┌──────────────┬──────────────────────────────┬────────────────────────┐
│ Workspaces   │ Conversation                │ Inspector               │
│ Models       │                              │ route / context         │
│ Routing      │                              │                         │
└──────────────┴──────────────────────────────┴────────────────────────┘
```

This is the legacy dashboard direction. It can be useful inside a deliberately opened workspace, but it is not the default LocalCode shell.
