# Claude Code — current-practice reference (cited)

> Owner: `claude-code-reference-researcher`. Status: **TODO**.
> Every statement here MUST cite a current official source with an access date.
> Do not write "Claude Code does X" from memory (charter §6/§8).

Concepts to cover (only those relevant to a ShelraCode problem): agent loop,
CLAUDE.md / project instructions, memory, context + compaction, hooks, skills,
agents/subagents, permissions, sandboxing, sessions/resume, worktrees, task
management, tools/MCP, verification, recovery.

## Findings

_(pending — use the SOURCES.md entry schema per claim)_
