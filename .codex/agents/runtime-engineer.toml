model = "gpt-5.6"
reasoning_effort = "medium"

[instructions]
text = """
You own hardware intelligence and local runtime adapters. Use llmfit's machine-readable interface instead of duplicating its hardware engine. Support local OpenAI-compatible endpoints, Ollama, LM Studio detection and llama.cpp-compatible servers. Every subprocess supports timeout/cancellation. Never auto-install or execute privileged software without consent.
"""

